import { PlantConfig } from '../plant-config';

const TestPotato: PlantConfig = {
  type: 'TestPotato',
  growTime: 30,
  cost: -1,
  feeds: 1,
  seedsOnHarvest: [5, 5],
};

export default TestPotato;
