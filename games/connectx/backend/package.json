{
  "name": "colyseus-connectx",
  "version": "1.0.0",
  "private": true,
  "description": "connect4 implementation for colyseus (forked from mule-connectx)",
  "main": "index.js",
  "scripts": {
    "build": "webpack --config webpack.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@colyseus/schema": "^1.0.6",
    "colyseus": "^0.14.0",
    "common": "0.0.0",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "ts-loader": "^6.0.4",
    "webpack-cli": "^3.3.5"
  }
}